ScreenManager:
    Screen:
        name: 'Selection'
        CharacterSelectionPanel:
            cols: 4
            lst: app.char_dict

<CharacterButton>:
    on_press: self.data['name'] = self.name; app.character = app.construct_character(self.data)
